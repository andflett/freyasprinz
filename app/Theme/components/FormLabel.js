const FormLabel = {
    baseStyle: ({ colorScheme }) => ({
        fontFamily: "Poppins, sans-serif",
        fontWeight: "600",
        fontSize: "sm",
        color: "blue.600"
    })
}

export default FormLabel


